---
firewalld_services:
  ssh:
    port: 22/tcp
    zone: public
    state: enabled
  http:
    port: 80/tcp
    zone: public
    state: enabled
  https:
    port: 443/tcp
    zone: public
    state: enabled
